## Portfolio view
4. Add portfolio metrics: Sharpe ratio, max drawdown, CAGR

## Screener
6. Historical screener queries (e.g. "show RSI < 30 on every day in the past year") — currently snapshot-only

## Single stock view
8. Candlestick chart option (currently line only)

## Universe / data
9. Search for tickers outside the existing universe (arbitrary Yahoo Finance lookup)
10. Replace vectorbt (vbt.YFData) in DataLoader with plain yfinance — vectorbt is the
    only remaining dependency on vbt now that backtesting is removed

## Housekeeping
11. Remove or archive old files no longer used on this branch:
    dashboard.py, backtest.py, strategies.py, generate_signals.py,
    test_signals_vs_backtest.py, tests/
